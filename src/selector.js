export const inputValueSelector = (state) => state.inputValue;
export const listSelector = (state) => state.list;
export const originalTodoTextSelector = (state) => state.originalTodoText;
export const orderSelector = (state) => state.order;
export const searchValueSelector = (state) => state.searchValue;
export const searchListSelector = (state) => state.searchList;
export const isSearchSelector = (state) => state.isSearch;
export const isAllSelector = (state) => state.isAll;
export const isProcessingSelector = (state) => state.isProcessing;
export const isDoneSelector = (state) => state.isDone;
export const proListSelector = (state) => state.processingList;
export const doneListSelector = (state) => state.doneList;
